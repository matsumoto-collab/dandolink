# 新セッション スターター指示文

新しい Cowork セッションを開いた時、以下の本文をそのまま最初のメッセージに貼り付けてください。
これだけで前セッションの状態がすべて引き継がれ、続きから作業できます。

---

## ↓ ここから貼り付け ↓

DandoLink の現場調査（図面）機能の続きを進めます。

【役割】
- 私はプログラミング超初心者です
- あなた（Cowork）は **プランナー / 設計役**。コードは書かず、Claude Code 用の指示文を作って渡してください
- 実装は別の Claude Code セッションが行います
- 私は本番 DB に接続中の環境で開発しているため、`prisma migrate` 系のコマンドは私が手動実行します

【プロジェクト】
- パス: `C:\Users\yushink\Desktop\yusystem`
- 設計書: `docs/SCAFFOLD_DRAWING_SPEC.md`
- 引き継ぎドキュメント: `docs/SCAFFOLD_DRAWING_HANDOFF.md` ← まずこれを読んでください
- ローカル: `npm run dev` → `http://localhost:3001/`

【最初にやってほしいこと】
1. `docs/SCAFFOLD_DRAWING_HANDOFF.md` を Read で読み、現状を把握する
2. 設計書 `docs/SCAFFOLD_DRAWING_SPEC.md` も必要に応じて参照する
3. 把握した状態を私に短くサマリーする（10〜20行）
4. 引き継ぎドキュメントの「次のタスク（A: DB 永続化）」を進めるための準備として、
   私が手動で実行する Prisma マイグレ手順と、それが完了した後の Claude Code 指示文の構成を提案する

【守ってほしいこと】
- Cowork からはコードを直接編集しない（指示文を作る）
- 既存機能を壊さない、修正は SiteSurvey 関連に留める
- DragInputController.tsx は温存（将来用）
- Vercel デプロイはしない

## ↑ ここまで貼り付け ↑

---

## 補足

このスターターは「次に DB 永続化（タスク A）から進めるとき」用です。
別のタスクから始めたい場合は、最後の段落を以下のように差し替えてください:

- **メタ情報フォームから**:
  > 引き継ぎドキュメントを読んだら、Phase 1 の残課題のうち「メタ情報フォーム（担当者・着時間・引継ぎ事項）」から始めたい。設計書 §3.6 を参考に、Claude Code への実装指示文を作ってください。

- **ピン・メモから**:
  > 引き継ぎドキュメントを読んだら、Phase 1 の残課題のうち「ピン・メモ機能」（設計書 §3.4）から始めたい。Claude Code への実装指示文を作ってください。

- **見積書連携から**:
  > 引き継ぎドキュメントを読んだら、Phase 1 の残課題のうち「見積書連携（足場面積の流し込み）」（設計書 §3.7）から始めたい。Claude Code への実装指示文を作ってください。

- **挙動の改善から**:
  > 引き継ぎドキュメントを読んだら、まず実機で `http://localhost:3001/` の現場調査を触ってみて、気付いた使いにくい点を3つほど挙げ、改善優先順位を一緒に整理してほしい。
